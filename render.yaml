# render.yaml
services:
  - type: web
    name: dress-fit-analyzer
    env: python
    region: oregon
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: gunicorn app:app
