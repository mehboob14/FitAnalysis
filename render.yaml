# render.yaml
services:
  - type: web
    name: dress-fit-analyzer
    env: python
buildCommand: |
  pip install -r requirements.txt
  playwright install
startCommand: gunicorn app:app

    region: oregon
